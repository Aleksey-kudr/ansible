#playbook install docker on ubuntu 20.04
- name: install docker
  hosts: all
  tasks:
    - name: install apt repository
      apt:
        become: true
        pkg:
        - ca-certificates
        - curl
        - gnupg
        state: absent
      register: out
    - debug:
        var: out

    #- name: add GPG key
      #apt_key:
